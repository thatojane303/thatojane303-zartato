{
  "name": "zartato",
  "version": "1.0.0",
  "description": "ZarTATO (ZRT) - BRETT model meme on Base. Trade-only on Aerodrome, educational, no intrinsic value.",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network baseMainnet"
  },
  "keywords": ["meme", "base", "aerodrome", "brett-model", "zartato"],
  "author": "Thato Jane",
  "license": "MIT",
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "discord.js": "^14.14.0"
  },
  "devDependencies": {
    "hardhat": "^2.19.0",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "ethers": "^6.10.0"
  }
}